# Configuration pour développement local
# PostgreSQL et API tournent en local

# Database (PostgreSQL local)
# Installez PostgreSQL : https://www.postgresql.org/download/
# Ou via Homebrew : brew install postgresql@16
DATABASE_URL="postgresql://scrappingscan:scrappingscan_password@localhost:5432/scrappingscan?schema=public"

# Server
PORT=3000
NODE_ENV=development
CORS_ORIGIN=*

# MyAnimeList API (optionnel)
MAL_CLIENT_ID=
MAL_CLIENT_SECRET=

# Legacy Supabase (optionnel)
SUPABASE_URL=
SUPABASE_KEY=
SUPABASE_EMAIL=
SUPABASE_PASSWORD=
